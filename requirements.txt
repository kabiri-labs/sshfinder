# Core port scanning and banner-based SSH validation run on the Python
# standard library alone. The dependencies below add stronger features.

# Recommended: host-key fingerprints, auth-method enumeration and shared-key
# correlation in --audit, plus full-handshake validation (--validate paramiko).
paramiko>=3.0

# Optional: half-open SYN scanning (--scan-method syn). Requires root.
# Install separately if you need it:  pip install scapy>=2.5
# scapy>=2.5
